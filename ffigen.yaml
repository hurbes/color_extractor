# Run with `dart run ffigen --config ffigen.yaml`.
name: ColorExtractorBindings
description: |
  Bindings for `src/color_extractor.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/color_extractor_bindings_generated.dart'
headers:
  entry-points:
    - 'src/color_extractor.h'
  include-directives:
    - 'src/color_extractor.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
